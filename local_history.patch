Index: pw-server/pw-gateway/src/main/java/com/op/gateway/controller/SiteBuilderAppProxyResource.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- pw-server/pw-gateway/src/main/java/com/op/gateway/controller/SiteBuilderAppProxyResource.java	(revision )
+++ pw-server/pw-gateway/src/main/java/com/op/gateway/controller/SiteBuilderAppProxyResource.java	(revision )
@@ -0,0 +1,27 @@
+package com.op.gateway.controller;
+
+import org.springframework.web.bind.annotation.RequestMapping;
+import org.springframework.web.bind.annotation.RestController;
+
+@RestController
+@RequestMapping("/app")
+public class SiteBuilderAppProxyResource {
+   /**
+    * Resource method that returns the portal asset.
+    * @param path App Asset Path
+    * @return Response
+    * @throws IOException IOException
+    */
+   @GET
+   @UnitOfWork
+   @Path("/{path:.*}")
+   public Response getAsset(@PathParam("path") final String path) throws IOException {
+       URL url = new URL(ConfigurationHolder.getInstance().getFrontendAppRootUrl() + "/" + path);
+
+       try (InputStream is = url.openStream()) {
+           return Response.ok().entity(IOUtils.toByteArray(is)).build();
+       } catch (IOException e) {
+           return Response.status(Response.Status.NOT_FOUND).entity(e).build();
+       }
+   }
+}
\ No newline at end of file
Index: pw-server/pw-gateway/src/main/java/com/op/gateway/controller/ApplicationProxyController.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- pw-server/pw-gateway/src/main/java/com/op/gateway/controller/ApplicationProxyController.java	(revision )
+++ pw-server/pw-gateway/src/main/java/com/op/gateway/controller/ApplicationProxyController.java	(revision )
@@ -7,7 +7,6 @@
 import org.springframework.http.HttpStatus;
 import org.springframework.http.ResponseEntity;
 import org.springframework.util.AntPathMatcher;
-import org.springframework.web.bind.annotation.PathVariable;
 import org.springframework.web.bind.annotation.RequestMapping;
 import org.springframework.web.bind.annotation.RequestMethod;
 import org.springframework.web.bind.annotation.RestController;
@@ -27,28 +26,18 @@
     /**
      * Resource method that returns the portal asset.
      * @return Response
-     * @throws java.io.IOException IOException
-     */
-//    @RequestMapping(value="/public", method = RequestMethod.GET)
-//    public ResponseEntity getAsset(final HttpServletRequest request) throws IOException {
-//        return this.getAsset("public/index.html", request);
-//    }
-
-    /**
-     * Resource method that returns the portal asset.
-     * @return Response
      * @throws IOException IOException
      */
     @RequestMapping(value="/**", method = RequestMethod.GET)
     public ResponseEntity getAsset(final HttpServletRequest request) throws IOException {
-        URL url = new URL(environment.getProperty("bsd.frontEndAppUrl") + "/" + request.getContextPath());
-
         String path = (String) request.getAttribute(
                 HandlerMapping.PATH_WITHIN_HANDLER_MAPPING_ATTRIBUTE);
         String bestMatchPattern = (String ) request.getAttribute(HandlerMapping.BEST_MATCHING_PATTERN_ATTRIBUTE);
 
         AntPathMatcher apm = new AntPathMatcher();
         String finalPath = apm.extractPathWithinPattern(bestMatchPattern, path);
+
+        URL url = new URL(environment.getProperty("bsd.frontEndAppUrl") + "/" + finalPath);
 
         try (InputStream is = url.openStream()) {
             return ResponseEntity.ok().body(IOUtils.toByteArray(is));
Index: pw-server/pw-gateway/src/main/resources/application.yml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- pw-server/pw-gateway/src/main/resources/application.yml	(revision )
+++ pw-server/pw-gateway/src/main/resources/application.yml	(revision )
@@ -1,31 +1,11 @@
 server:
   port: ${PORT:9023}
 
-bsd:
-  frontEndAppUrl: http://localhost:7358
-  frontendAppIndexRootUrl: http://localhost:8080/app/public
 
 eureka:
   client:
     serviceUrl:
       defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
-
-
-logging:
-  level:
-    ROOT: INFO
-    org.springframework.data.rest.webmvc: 'DEBUG'
-    org.springframework.boot.autoconfigure.security: 'WARN'
-    org.springframework.context.support: 'WARN'
-    org.springframework.jdbc.datasource: 'WARN'
-    org.springframework.boot.context: 'WARN'
-    org.springframework.security.web: 'WARN'
-    org.springframework.http: 'INFO'
-    org.springframework.scheduling.concurrent: 'WARN'
-    org.springframework.beans.factory.config: 'WARN'
-    org.springframework.integration.config: 'WARN'
-    org.springframework.web.servlet.mvc.method.annotation: 'WARN'
-    org.springframework.beans.factory.support: 'WARN'
 
 ---
 spring:
